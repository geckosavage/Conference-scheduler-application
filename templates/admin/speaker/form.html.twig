{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="card" style="max-width: 780px;">
        <h1>{{ title }}</h1>
        {{ form_start(speakerForm) }}
            {{ form_widget(speakerForm) }}
            <button type="submit">Save Speaker</button>
        {{ form_end(speakerForm) }}
    </div>
{% endblock %}
